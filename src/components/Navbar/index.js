import React from "react";
import { NavbarContainer, Logo } from "./styled";

const Navbar = () => {
  return (
    <NavbarContainer>
      <Logo src="./images/logo.png" alt="logo" />
    </NavbarContainer>
  );
};

export default Navbar;
